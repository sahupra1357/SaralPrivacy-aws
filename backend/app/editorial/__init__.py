"""Editorial domain logic: briefings, the verified blog, and the daily briefing pipeline.

Routes live in `app/api/routes/{briefings,blog}.py`, queries in `app/crud/editorial.py`,
the scheduled job in `app/jobs/editorial.py`. This package holds everything in between.
"""
