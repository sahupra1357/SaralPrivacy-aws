# Module status

Written by the orchestrator only. Builders report in `docs/build/status/<module>.md`.
Statuses: `todo` · `inventory` · `building` · `static-clean` · `integrated` · `verified` · `accepted`.
Everything is uncommitted working-tree state until wave 5 sign-off.

| Module | Wave | Status | Orchestrator notes |
|---|---|---|---|
| core | 0 | integrated | skeleton + contracts; migration 0001; static checks clean on host |
| auth | 1 | integrated | 3 routers + 5 models registered; pytest_plugins added; review migration for spurious login_attempts drop; ops.blogger_accounts must exist (admin module) |
| forms | 1 | integrated | router + 6 models registered; phonenumbers dep added; email attachments added to core; migration must add 2 CHECKs + 2 indexes (see status/forms.md) |
| assessments | 1 | integrated | router+model registered; migration deferred until wave 1 complete (Docker needed); note: raw SQL to ops.subscribers/consent_log to be collapsed onto forms crud |
| notices | 1 | integrated | router+models registered; core TimestampMixin bug fixed; unmodelled tables (notice_runs, business_profiles, dsar_requests, chat_feedback) to be created at migration time |
| chat | 1 | integrated | router + ChatFeedback registered; llm.temperature now optional; app/data/*.json shipped in image; POST /chat needs >=60s proxy/ALB timeout |
| editorial | 2 | integrated | 2 routers + 2 models + daily job registered; 11 settings keys added; /api/briefings/* rewritten straight to backend (n8n + emailed approve links); resvg/puppeteer/chromium removed from frontend |
| outreach | 2 | integrated | 3 routers + 2 models + 2 jobs registered; webhook kept at old URL via next.config rewrite + svix headers in proxy; daily cap default restored to 50; migration needs 3 CHECKs + created_at_attr index (status/outreach.md) |
| admin | 2 | integrated | 3 routers + 6 models + aeo-panel & seo-inspect jobs registered; google-auth added; SEO CLI, provision.ts, seo workflow moved to _backup |
| integration | 3 | done except data load | dead code/packages removed, CSP/images from asset URL, CloudFront geo + viewer IP, start-up warm-up, typed client, Terraform v2 (3 services, CloudFront, S3, RDS wired), CI builds 2 images; data load waits on user access |
| verification | 4 | verified | backend 744/744 (95%), vitest 244/244, node 175/175; ruff/format/mypy strict/tsc clean; eslint: 0 new problems (baseline from original code unchanged); fresh-machine bring-up verified |
| manual acceptance | 5 | todo | user |

## Orchestrator log

- 2026-09-17 — plan approved, skills and agents created, no code changed.
- 2026-09-18 — wave 0 done: webapp→frontend (mv), backend scaffold, compose, Makefile, core contracts + tests. Nothing committed.
- 2026-09-18 — assessments integrated (registrations added). Migrations for wave 1 will be generated together once Docker Desktop is running.
- 2026-09-18 — notices integrated. auth/forms/chat builders were cut off by the API spend limit mid-build (auth near the end, forms mid-frontend, chat at crud). Their partial files remain in the tree; resume them, do not restart from scratch.
- 2026-09-18 — auth integrated (resumed builder). Open: 409 invite string vs bloggers route; no forgot-password UI; revoked token still renders admin shell until API 401s.
- 2026-09-18 — forms integrated (resumed builder). Open: consent_source 'manual' label for template/whitepaper surfaces; guide PDFs still on Vercel Blob URLs.
- 2026-09-18 — chat integrated (resumed builder, finished after spend-limit stops). Wave 1 code complete.
- 2026-09-18 — wave 1 migration `019fd8cf33f7` generated, reviewed and applied to the local db: 12 app + 7 ops tables; manual CHECKs (subscribers consent_source/status), status index, and unmodelled notice_runs/business_profiles/dsar_requests. alembic/env.py now refuses to autogenerate drops of unmodelled objects. `alembic check` clean. First admin seeded. 30 API endpoints registered.
- Wave 2 prerequisites: admin must model ops.blogger_accounts (auth reads it via text()); outreach/admin should reuse forms' render_survey_report and crud.forms.upsert_subscriber; chat_feedback already owned by chat.
- 2026-09-18 — user decisions: (1) consent_source "manual" for template/white-paper sign-ups is correct; (2) guide PDFs must be self-hosted (orchestrator downloads them into frontend/public/guides/pdf/ and repoints GUIDE_PDF_URLS); (3) city/country/region geo will be supplied by CloudFront viewer headers in the AWS phase (wave 3 note: proxy must forward CloudFront-Viewer-Country/-City/-Country-Region and backend must read them).
- 2026-09-18 — wave 2 launched: editorial, outreach, admin.
- 2026-09-18 — outreach integrated. Open: SMTP Message-ID won't match Resend email_id, so webhook events mostly insert rather than update (suppression unaffected).
- 2026-09-18 — admin and editorial integrated. Wave 2 code complete; migration next.
- 2026-09-18 — wave 2 migration `c6358c0b0979` applied locally (+ outreach CHECKs/index). Totals: 64 endpoints, 29 tables (app 13, ops 16), 5 worker jobs (editorial_daily_briefing 03:30, aeo-panel Mon 03:30, outreach-send 04:00, briefing-send 04:30, seo-inspect Mon 04:00 UTC). `alembic check` clean.
- Also done early (wave 3 items): removed @resvg/resvg-js, puppeteer-core, @sparticuz/chromium from frontend; moved lib/fonts, lib/seo, tools/seo, tools/auth/provision.ts, seo-inspect workflow to _backup.
- Open for user: should the backend block bloggers from publishing (today only the UI blocks it)?
- 2026-09-19 — user asked to run all tests and bring the app up. Results: backend pytest 736/736 (95% line coverage), frontend vitest 243/243, node:test (via tsx) 183/183.
  Fixes during the run: test session fixture → SQLAlchemy join_transaction_mode="create_savepoint"; root conftest pytest_plugins actually added; scripts/test.sh creates the test db before connecting; admin timestamps serialised in JS toISOString shape; jsdom stubs (ResizeObserver etc.); several test selectors aligned with real components; test:lib runs under tsx (pre-existing extensionless imports); discovery golden fixture regenerated (generated source byte-identical).
  Docker: frontend Dockerfile trimmed (no Chromium/fonts); package-lock regenerated with container npm; MinIO images now from quay.io (Docker Hub dropped minio/minio).
  Smoke test through :3000: 17 pages/assets 200; contact + subscribe write rows and send mail to mailcatcher; notice PDF 2 pages; admin login → TOTP enroll step with path-scoped cookie; wrong password 401; /admin redirects to login; worker scheduled 5 jobs.
- 2026-09-19 — root cleanup (user request): root public/ (stale duplicate), templates/, tools/, workflows/ (legacy n8n/Gmail WAT pipeline), WAT.md, requirements.txt, vercel.json(.backup) and 15 uncited specs → _backup/. 19 specs cited by frontend code → frontend/docs/specs/. roadmap/ → backend/app/editorial/data/roadmap/ (now the default CSV fallback). supabase/ → _backup/supabase/ (19 comment/doc references repointed). Editorial tests 114/114 after the config change.
- 2026-09-19 — wave 3. Moved to _backup: frontend lib/{db,appwrite,email,resendClient,sendGateway,email-templates,adminSession,tokens}, 6 Appwrite/Blob scripts, frontend/templates, vercel.json(.backup), .build-trigger x2, Vercel deploy.yml x2, infra cron.tf + lambda. lib/abuseGuard.ts reduced to HONEYPOT_FIELD (original backed up). Uninstalled node-appwrite, @vercel/blob, resend, svix, ai, @ai-sdk/anthropic (lock regenerated with container npm). Kept: @supabase/supabase-js + tools/migrate-supabase + backend/legacy/supabase until data is loaded; @vercel/analytics pending user decision.
  next.config: remotePatterns + CSP img-src from PUBLIC_ASSET_BASE_URL (build arg), connect-src 'self' only. deps.client_geo (CloudFront-Viewer-* → x-vercel-ip-* fallback) used by forms/assessments/notices/chat; deps.client_ip prefers CloudFront-Viewer-Address; proxy forwards those headers. /api/revalidate accepts type=layout; frontend entrypoint scripts/start-with-warmup.mjs. Storage defaults empty (real S3) — MinIO only via .env/compose. make client fixed; src/client generated (64 paths).
  Terraform v2 validates: ECR web+api, ECS web/api/worker, Cloud Map backend.saralprivacy.internal, backend SG, RDS only from backend SG, S3 infographics/* public-read, CloudFront (+us-east-1 cert, origin.<domain>), ALB ingress CloudFront prefix list, worker-down alarm, OIDC for 2 repos/3 services. deploy-aws.yml builds 2 images, rolls api first. AWS_DEPLOYMENT_PLAN v2; README rewritten.
  Verified on rebuilt Docker stack: warm-up 3x200, CSP names MinIO origin, contact via proxy stores CloudFront IP/city/country/region, Hindi PDF, MinIO put + public GET, /admin guard. tsc clean, ruff clean.
- Open: data export access; blogger publish enforcement; Vercel Analytics keep/remove.
- 2026-09-19 — user will handle data migration. Removed data/seed/ (created empty in wave 0, not in the original repo, so deleted rather than backed up) and the `make seed` target, .gitignore entry and doc references.
- 2026-09-19 — user handles data migration: moved frontend/tools/migrate-supabase, backend/legacy/supabase (→ _backup/supabase), SUPABASE_MIGRATION_SPEC.md to _backup; uninstalled @supabase/supabase-js. Code comments now cite _backup/supabase/.
- 2026-09-19 — wave 4. Fixes: Vercel Analytics rendered only when VERCEL=1 (404'd off Vercel); ruff format applied (61 files); mypy strict clean (real fixes: handoff TypeGuard, _HasUrl protocol property, aeo typed base, PostgresDsn, pdf margins; scoped overrides for SQLModel Field overloads, untyped libs, relaxed test profile); 3 eslint problems introduced by the rebuild fixed (the ~270 remaining exist identically at HEAD); passlib → bcrypt (passlib imports `crypt`, removed in Python 3.13; hashes compatible); HTTP_413 constant renamed; MinIO bucket now created by backend prestart (storage.ensure_bucket) — one-shot minio-init removed because `up --wait` fails on exited containers; mailcatcher + minio start by default so `make up` is the whole local bring-up.
  Verified from scratch with only .env.example in a throwaway compose project: migrations 0001→019fd8cf33f7→c6358c0b0979, admin seeded, bucket created, warm-up ok, `up -d --wait` exit 0, pages/login/contact→inbox/PDF/storage public read all OK. User's stack restored with its data.
  docs/build/MANUAL_TEST_CHECKLIST.md written (57 items) for wave 5.
- 2026-09-19 — language toggle missing locally: .env had NEXT_PUBLIC_SHOW_HINDI=1 (gate needs "true") and NEXT_PUBLIC_* never reached the Docker build. Fixed: value "true" in .env/.env.example; build arg in frontend/Dockerfile, docker-compose.yml and deploy-aws.yml (repo variable NEXT_PUBLIC_SHOW_HINDI). Verified toggle in served HTML for /, /learn, /hi.
- 2026-09-19 — upstream sync #1: ported origin/main commits 1bb1a84 (#41 hero tap #2 + prefill), 13b6629 (#44 score dial), 1bea332 (#42 text-2xs/animate-fade-up in @theme) onto the working tree. Method: read-only `git diff ad352de origin/main`, paths webapp/→frontend/ (HERO_TAP2_SPEC.md → frontend/docs/specs/), applied with `patch` (0 fuzz/rejects), no git state changed. 11 files byte-identical to origin/main; the 12 assessment clients differ only by the proxy fetch line. tsc clean; vitest 244/244; node 193/193 (+18 upstream tests); image rebuilt; served CSS has .text-2xs; sample score 42 as live. Rollback copies in the session scratchpad (pre-upstream3/).
  Sync baseline for the next port: origin/main 1bea332 (next sync = `git diff 1bea332 origin/main`).
