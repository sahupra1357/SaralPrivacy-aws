# Secrets
.env
credentials.json
token.json

# Temporary files
.tmp/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
*.pem
# MS Office owner/lock temp files (e.g. ~$Document.docx)
~$*

# Node / Next.js
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/.next/
/out/
/build

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env files
.env*
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
node_modules

# Search Console service-account key + SEO inspection reports (tools/seo)
.gsc-service-account.json
webapp/tools/seo/reports/

# Rebuild (frontend/ + backend/ + compose)
backend/.venv/
backend/.pytest_cache/
backend/.mypy_cache/
backend/.ruff_cache/
backend/htmlcov/
backend/.coverage
frontend/test-results/
frontend/playwright-report/
frontend/openapi.json
_backup
frontend/public/assets/dpdpa-white-paper-2025.pdf
frontend/public/guides/pdf/.
# Next build output anywhere in the tree (the /.next/ above only matches the root).
.next/
# Large language guides — served from storage, not the repo.
dpdpa-guide-*.pdf
# Local agent config (skills, agents, orchestration).
.claude/

# Terraform: provider binaries are hundreds of MB, state holds secrets.
.terraform/
*.tfstate
*.tfstate.*
crash.log
